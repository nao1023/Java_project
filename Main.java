import java.util.*;

public class Main {
  public static void main(String[] args) {
    // double value = 32.0;
    // String bytes =
    // String.format("%64s", Long.toBinaryString(Double.doubleToLongBits(value))).replace("", "0");
    // System.out.println(bytes);

    // Scanner sc = new Scanner(System.in);
    // double x = Double.parseDouble(sc.nextLine());
    // int IntegerPart = (int)x;
    // x -= (double)IntegerPart;
    // String bin = Integer.toBinaryString(IntegerPart);
    // int []data = {0,1};
    // for(int i = 0; i < 20; i++){ 
    //     if(i == 0){
    //         System.out.print(bin + ".");
    //     }
    //     else{
    //         System.out.print(data[(int)x % 2]);
    //     }
    //     x *= 2.0;
    // }

    // for (int i=1;i<20;i++){
    //   double result = Math.pow(13, i);
    //   System.out.println(result);
    // }
    double i=0.8;
    int x=(int)i;

    System.out.println(x);
  }
}